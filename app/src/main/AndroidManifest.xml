<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.appsistemainventario">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppSistemaInventario"
        tools:targetApi="31">

        <!-- 🟢 Actividades principales -->
        <activity
            android:name=".UI.DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🧾 Otras actividades -->
        <activity android:name=".UI.RegistrarProductoActivity" android:exported="false" />
        <activity android:name=".UI.RegistrarCategoriaActivity" android:exported="false" />
        <activity android:name=".UI.RegistrarProveedorActivity" android:exported="false" />
        <activity android:name=".UI.RegistrarMovimientoActivity" android:exported="false" />

        <!-- 🆕 Nueva actividad: Lista de Productos -->
        <activity android:name=".UI.ItemsProductoActivity" android:exported="false" />

    </application>
</manifest>
